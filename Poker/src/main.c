
#include "main.h"

int main() {
    //gfx_Begin();
    //gfx_SetPalette(gfx_pal, sizeof_gfx_pal, 0);
    //gfx_FillScreen(0);
    //gfx_SetTextConfig(gfx_text_noclip);

    srand(time(NULL));

    play();

    //while (!os_GetCSC());

    //gfx_End();

    return 0;
}